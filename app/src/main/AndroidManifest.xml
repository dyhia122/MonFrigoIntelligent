<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <!-- ⚠️ Permission caméra obligatoire -->
    <uses-permission android:name="android.permission.CAMERA" />

    <!-- (optionnel) nécessaire pour l’écriture si tu fais des sauvegardes -->
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE"
        android:maxSdkVersion="28" />

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.MyApplication">

        <!-- Écran principal -->
        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:label="@string/app_name"
            android:theme="@style/Theme.MyApplication">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- Écran frigo -->
        <activity
            android:name=".FrigoActivity"
            android:theme="@style/Theme.AppCompat.Light.NoActionBar" />

        <!-- Menu -->
        <activity
            android:name=".MenuScreen"
            android:theme="@style/Theme.AppCompat.Light.NoActionBar" />

        <!-- 🚀 Scanner (à AJOUTER absolument) -->
        <activity
            android:name=".ScanActivity"
            android:exported="false"
            android:theme="@style/Theme.AppCompat.Light.NoActionBar" />

        <!-- Ajouté : Corbeille -->
        <activity
            android:name=".CorbeilleActivity"
            android:exported="false"
            android:theme="@style/Theme.AppCompat.Light.NoActionBar" />

        <!-- Ajouté : Compte -->
        <activity
            android:name=".CompteActivity"
            android:exported="false"
            android:theme="@style/Theme.AppCompat.Light.NoActionBar" />

    </application>

</manifest>